name: "Create a SLSA Source Track VSA"

on:
  workflow_call:

jobs:
  create_vsa:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
    - name: vsa
      # TODO: only generate VSAs for official pushes? IDK
      # TODO: Update to point at the SLSA one...
      uses: tomhennen/slsa-source-poc/actions/vsa_creator@main
